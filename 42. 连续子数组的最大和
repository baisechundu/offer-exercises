# -*- coding:utf-8 -*-
class Solution:
    def FindGreatestSumOfSubArray(self, array):
        res, maxmum = 0, None
        for i in array:
            res += i
            maxmum = max(maxmum, res)
            if res < 0:
                res = 0
        return maxmum
